<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/colorPrimary"
    android:padding="16dp">

    <com.eftimoff.androipathview.PathView
        android:id="@+id/anf_path_image_view"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        app:svg="@raw/writing"
        app:pathWidth="2dp"
        android:backgroundTint="@color/colorWhite"/>

    <TextView
        android:id="@+id/anf_dialog_message_textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:fontFamily="@font/roboto_regular"
        android:textSize="20sp"
        android:layout_below="@+id/anf_path_image_view"
        android:layout_marginTop="16dp"
        android:textColor="@color/colorWhite"
        tools:text="Would you like to add some description to your article?" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="38dp"
        android:orientation="horizontal"
        android:layout_below="@id/anf_dialog_message_textView"
        android:layout_marginTop="32dp">

        <Button
            android:id="@+id/anf_dialog_positive_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Yes"
            android:layout_weight="1"
            android:textColor="@color/colorWhite"
            android:background="@drawable/bg_rectangle_stroke"/>

        <Button
            android:id="@+id/anf_dialog_negative_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No"
            android:layout_weight="1"
            android:layout_marginStart="10dp"
            android:textColor="@color/colorWhite"
            android:background="@drawable/bg_rectangle_stroke"/>
    </LinearLayout>

</RelativeLayout>